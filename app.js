function Book(title, author, totalPage, completedPages, completed) {
  this.title = title;
  this.author = author;
  this.totalPage = totalPage;
  this.completedPages = completedPages;
  this.completed = completed;
}
